{% extends 'layout.html.twig' %}

{% block content %}
    <h5>Votre panier</h5>
    <table class="table">
        <thead>
        <tr>
            <th scope="col">ID utilisateur</th>
            <th scope="col">Cout total</th>
            <th scope="col">Token Stripe</th>
            <th scope="col">ID produits</th>
        </tr>
        </thead>
        <tbody>
        {% for order in orders %}
            <tr>
                <th scope="row">{{ order.user.id }}</th>
                <td>{{ order.total }} €</td>
                <td>{{ order.token }}</td>
                <td>{{ order.description }}</td>
            </tr>

        {% endfor %}
        </tbody>
    </table>
{% endblock %}

{% block page_title %}

{% endblock %}

